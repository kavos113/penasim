syntax = "proto3";

package com.example.penasim;

import "google/protobuf/descriptor.proto";

message TableOptions {
  string table_name = 1;
}

message FieldOptions {
  optional bool is_primary_key = 1;
  optional bool is_index = 2;
  optional ForeignKey foreign_key = 3;
}

message ForeignKey {
  string parent_table = 1;
  string parent = 2;
}

extend google.protobuf.MessageOptions {
  optional TableOptions table_options = 50000 [targets = TARGET_TYPE_MESSAGE];
}

extend google.protobuf.FieldOptions {
  optional FieldOptions field_options = 50001 [targets = TARGET_TYPE_FIELD];
}